apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookshop-ui
spec:
  template:
    spec:
      containers:
        - name: bookshop-ui
          resources:
            requests:
              memory: 756Mi
              cpu: "0.1"
            limits:
              memory: 756Mi
              cpu: "2"
