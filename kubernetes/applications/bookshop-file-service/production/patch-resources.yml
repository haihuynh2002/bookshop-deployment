apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
spec:
  template:
    spec:
      containers:
        - name: file-service
          resources:
            requests:
              memory: 756Mi
              cpu: "0.1"
            limits:
              memory: 756Mi
              cpu: "2"
